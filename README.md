# 復習用メモ（GitHub 練習）

このフォルダは **Git / GitHub の操作を復習するため** の練習用です。  
README を編集したり、メモを追加したりして、`add` → `commit` → `push` の流れを繰り返し練習しましょう。

---

## 1. Git の復習

### よく使うコマンド

| コマンド | やること |
|----------|----------|
| `git status` | 変更されたファイルを確認する |
| `git add .` または `git add ファイル名` | 変更をステージング（コミットの準備） |
| `git commit -m "メッセージ"` | 変更を記録する |
| `git push` | GitHub に送る |
| `git pull` | GitHub から最新を取ってくる |

### 初回や新しいフォルダでやること

```bash
git init
git add .
git commit -m "最初のコミット"
git remote add origin https://github.com/自分のユーザー名/リポジトリ名.git
git branch -M main
git push -u origin main
```

---

## 2. HTML の復習

学習ログで触れた基本をまとめます。

- **`<!DOCTYPE html>`** … この文書が HTML5 ですよ、という宣言
- **`<html lang="ja">`** … ページの言語を日本語に指定
- **`<h1>`** … いちばん大きい見出し（h2, h3… と小さくなる）
- **`<p>`** … 段落（paragraph）

---

## 3. 練習の流れ（今日やること）

1. この README を開いて、下の「復習メモ」に今日学んだことや思い出したことを 1 行でも書く
2. ターミナルでこのフォルダ（またはリポジトリのルート）に移動する
3. `git add .`
4. `git commit -m "復習メモを追加"`
5. `git push`

何度でも繰り返して、手順に慣れていきましょう。

---

## 4. Markdown をひとつずつ練習

ここでは **リンク → コード → リスト** の順に、1つずつ試していきます。  
下の「やってみよう」に自分で書いて保存し、GitHub でプレビューを確認してみてください。

### ステップ1: リンク

**書き方:** `[クリックしたときの文字](URL)`

**例:**
- [GitHub](https://github.com) へのリンク
- [復習用READMEの先頭](#復習用メモgithub-練習) （同じページ内の見出しへ）

**やってみよう:** 下の行を編集して、自分がよく見るサイトへのリンクを1つ作ってみる。

- ここにリンクを書く: 
[github](https:github.com) 
[復習用READMEの先頭](#2-html-の復習)

[youtube](https:youtube.com)
[gitの練習ページ](#1-git-の復習)





### ステップ2: コード（「コマンドっぽく見せる」書き方）

**なぜ使う？**  
「git add と打って」のように、**コマンドやプログラムの一部**を、普通の文章と区別して表示したいときに使います。  
→ 読み手が「これは入力するものだな」と分かりやすくなります。

---

**パターンA: 1行だけ表示したいとき**

- **やること:** その文字の **前と後** を、バッククォート（キーボードの `` ` `` ・Shift＋@の左にあるキー）で1つずつ囲む。
- **書くとき:** バッククォート1個 → 半角で `git status` と打つ → バッククォート1個、の順。
- **見た目:** プレビューでは「git status」の部分が背景付きで表示される。

**パターンB: 複数行をまとめて表示したいとき**

- **やること:** コードの **1行上** に `` ``` `` を3つ並べて、その次に言語名（例: bash）。コードの **最後の行の下** に `` ``` `` を3つ並べる。
- **書くとき:**

````
```bash
git add .
git commit -m "メッセージ"
```
````

- **見た目:** 上の「Git の復習」にあるような、灰色のブロックになる。

（パターンBは「長いコードの例」を書くとき用。まずはパターンAだけ覚えてもOKです。）

**やってみよう:** 下の行に、自分がよく使うコマンドを **パターンA** で1つ書いてみる（前後を `` ` `` で囲む）。

- ここに書く: `cd my-learning-log`
---

### ステップ3: リスト（箇条書き・番号）

**箇条書き:** 行頭に `-` または `*` とスペース

- 項目1
- 項目2

**番号リスト:** 行頭に `1.` `2.` とスペース（数字は自動で揃う）

1. 最初にやること
2. 次にやること

**やってみよう:** 下に「今日やったこと」を箇条書きで3つ書いてみる。

- 
- 
- 

---

## 復習メモ（ここに追記して push の練習）

- （例）2月○日: Git の add → commit → push の流れを復習した
復習用のファイル作成、div、head、bodyの意味を知った
gitにpushする練習をして覚える